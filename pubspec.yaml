name: pos_clean
description: TipidPOS clean Flutter app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.4
  intl: ^0.19.0
  mobile_scanner: ^6.0.2
  image_picker: ^1.1.2
  file_picker: ^8.0.7
  google_fonts: ^6.2.1
  permission_handler: ^11.4.0
  print_bluetooth_thermal: ^1.1.9
  shared_preferences: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # 🔥 UNCOMMENT kung may assets ka
  # assets:
  #   - assets/
